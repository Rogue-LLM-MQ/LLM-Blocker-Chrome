{
  "type": "object",
  "properties": {
    "domains": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "id": { "type": "integer" },
          "domain": { "type": "string" },
          "policy": { "type": "string", "enum": ["allow", "warn", "block"] },
          "provider": { "type": "string" }
        },
        "required": ["id", "domain", "policy", "provider"]
      }
    }
  },
  "required": ["domains"]
}
